typeset -r INSERT_COLUMNS="branchno,customerno,articleno,quota,validity,datefrom,dateto";
typeset -r FILE_FIELD_DELIMITER=";";
typeset -r OUTPUTFILENAME_SUFFIX="splitquota_";
typeset -r INPUTFILE_PATH="/software/wss/fr/ksc/load/data";
typeset -r OUTPUTFILE_PATH="${INPUTFILE_PATH}/splitted_quota"
typeset -r OUTPUTFILE_SQL_PATH="${INPUTFILE_PATH}/sqlfile.sql"
typeset -i JUNK_SIZE=10000; #10000 should be max! creates a lot of files but otherwise sql statement will not work efficiently